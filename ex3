#include <stdio.h>

int main() {
    
    char modo;

    while (1) {
        printf("\n1. Secar; \n2. Enxaguar e Secar; \n3. Lavar, Enxaguar e Secar; \n4. Encerrar progrma de lavagem. \nDigite 1 dos modos: ");

        modo = getchar();
        getchar();

        switch (modo) {
            case '3': 
            printf ("\nLavando...");
            case '2'4:
            printf ("\nEnxaguando...");
            case '1':
            printf ("\nSecando...\n");
        }
    }
                if (modo == 4) {
                printf ("Máquina desligada!");
                }
return 0;
}
